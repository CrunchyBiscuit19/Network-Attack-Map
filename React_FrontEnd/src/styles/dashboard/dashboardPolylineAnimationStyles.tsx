// Material UI
import { makeStyles } from "@material-ui/core";

// Styling options to use for elements within DataTable component.
const useDashboardPolylineAnimationStyles = makeStyles(() => ({}));

export { useDashboardPolylineAnimationStyles };
